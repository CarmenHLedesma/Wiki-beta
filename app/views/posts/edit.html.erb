<%- model_class = Post -%>

<% content_for :title, 'wiki-beta' %>
<% navigation_add @post.title, 'root_path' %>

<div class="page-header">
  <h1><%=t '.title', :default => [:'helpers.titles.edit', 'Edit %{model}'], :model => model_class.model_name.human.titleize %></h1>
</div>
<%= render :partial => 'form' %>
